# Environment Variables Example
# Copy this file to .env and fill in your values

# TVDB API key - MANDATORY
# Get from https://thetvdb.com/api-information
# Example: TVDB_API_KEY=your_tvdb_api_key_here
TVDB_API_KEY=

# OMDb API Key - OPTIONAL
# Used for additional metadata - Get from http://www.omdbapi.com/apikey.aspx
# Example: OMDB_API_KEY=your_omdb_api_key_here
OMDB_API_KEY=

# Base URL for the addon - OPTIONAL
# Examples with or without domain:
# http://localhost
# https://xyz.com/xxx/yyy
# https://tvdb-addon.xyz.com
BASE_URL=http://localhost

# Server port configuration  - OPTIONAL
PORT=3000

# Admin API KEY  - OPTIONAL/RECOMMENDED
# Used for admin operations like clearing cache, updating metadata, etc.
# Set your own secure key to prevent unauthorized access
# Example: ADMIN_API_KEY=your_secure_admin_api_key_here
ADMIN_API_KEY=

# MongoDB connection string - OPTIONAL/RECOMMENDED
# If no MONGODB_URI is set, cache "memory" will be used
# Replace with your MongoDB credentials and URL
# Get from your MongoDB Atlas (free tier) or local MongoDB instance
# Example: MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net
MONGODB_URI=

# Cache Configuration - MANDATORY
# Options: memory, hybrid, mongodb
# memory: In-memory only (fast, no persistence) --> Default if no CACHE_TYPE is set
# hybrid: L1 (memory) + L2 (MongoDB) - RECOMMENDED for production
# mongodb: MongoDB only (slower but full persistence)
CACHE_TYPE=hybrid

# Logging Configuration - OPTIONAL
# Options: error, warn, info, debug
# error: Only error messages
# warn: Error and warning messages  
# info: Error, warning, and info messages (default)
# debug: All messages including debug output
LOG_LEVEL=info